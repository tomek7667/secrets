meta {
  name: 15 - Cleanup - Delete certificates
  type: http
  seq: 15
}

delete {
  url: {{burl}}/api/certificates/test-self-signed-cert
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{admin_token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
}

tests {
  test("Certificate deletion should succeed", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

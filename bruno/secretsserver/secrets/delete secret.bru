meta {
  name: delete secret
  type: http
  seq: 5
}

delete {
  url: {{burl}}/api/secrets?key={{secret_key}}
  body: none
  auth: inherit
}

params:query {
  key: {{secret_key}}
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.code: eq 200
  res.body.data: isNull
}

tests {
  test("should return 200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("should return success response", function() {
    expect(res.getBody().success).to.equal(true);
  });
  
  test("should return null data", function() {
    expect(res.getBody().data).to.be.null;
  });
}

docs {
  Example Response:
  ```json
  {
    "code": 200,
    "data": null,
    "message": "Success",
    "statusText": "OK",
    "success": true,
    "ts": "2025-12-06T15:17:11.954254007Z"
  }
  ```
}

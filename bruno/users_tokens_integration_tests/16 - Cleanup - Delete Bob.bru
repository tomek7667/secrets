meta {
  name: 16 - Cleanup - Delete Bob
  type: http
  seq: 16
}

delete {
  url: {{burl}}/api/users/{{bob_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{admin_token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
}

tests {
  test("User deletion should succeed", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

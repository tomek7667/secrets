meta {
  name: 12 - Cleanup - Delete expired token
  type: http
  seq: 12
}

delete {
  url: {{burl}}/api/tokens/{{expired_token_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{admin_token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
}

tests {
  test("Token deletion should succeed", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
